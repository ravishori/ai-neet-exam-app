[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    unit: fast tests with no external corpus dependencies
    integration: service/database tests with controlled fixtures
    real_corpus: uses actual NEET StudyMaterial pilot PDFs (optional/slow)
    e2e: full HTTP workflow tests
